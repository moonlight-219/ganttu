{
  "name": "gantt",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @gantt/playground dev",
    "lint": "tsc -p tsconfig.base.json --noEmit",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "benchmark": "pnpm --filter @gantt/playground benchmark",
    "publish": "pnpm -r --filter @gantt/core --filter @gantt/vue-gantt publish",
    "publish:force": "pnpm -r --filter @gantt/core --filter @gantt/vue-gantt publish --no-git-checks"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^25.0.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8",
    "vue": "^3.5.13",
    "vue-tsc": "^2.2.0"
  },
  "packageManager": "pnpm@9.15.0"
}
