{
  "name": "ct-gantt-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit",
    "benchmark": "vite build && node ./benchmarks/generate-report.mjs"
  },
  "dependencies": {
    "ct-gantt-core": "workspace:*",
    "ct-gantt-vue": "workspace:*",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
